function projectsCreation(name, countProjects) {
    let timeForOneProject = 3;
    let totalTime = countProjects * timeForOneProject;
    console.log(`The architect ${name} will need ${totalTime} hours to complete ${countProjects} project/s.`);
}