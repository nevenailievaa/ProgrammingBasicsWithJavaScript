function petShop(dogCansCount, catCansCount) {
    let dogFoodCost = dogCansCount * 2.5;
    let catFoodCost = catCansCount * 4;
    
    let sum = dogFoodCost + catFoodCost;
    console.log(sum);
}